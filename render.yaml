services:
  - type: web
    name: market-view
    runtime: docker
    plan: starter
    envVars:
      - key: PORT
        value: "3000"
      - key: NODE_ENV
        value: production
    disk:
      name: market-view-data
      mountPath: /app/data
      sizeGB: 1
